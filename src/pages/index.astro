---
import "../styles/global.css";
---

<html lang="gl">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta
			name="google-site-verification"
			content="L4SLkfdzHtVhBgKdSpuY53IMtpQCImZmNgYPvAOvGbI"
		/>
		<link rel="icon" type="image/png" href="/favicon.png" sizes="32x32" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="shortcut icon" href="/favicon.ico" />
		<title>somiiux</title>
		<style>
			/* Fade-in dende negro ao entrar */
			body {
				opacity: 0;
				animation: fadeInFromBlack 1.2s ease forwards;
			}

			@keyframes fadeInFromBlack {
				from {
					background-color: #000;
					opacity: 0;
				}
				to {
					background-color: #0a0a0a;
					opacity: 1;
				}
			}

			body.locked {
				pointer-events: none;
			}

			.focus-in-somiiux {
				animation: focus-in-expand 1.2s
					cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
			}

			@keyframes focus-in-expand {
				0% {
					filter: blur(14px);
					opacity: 0;
				}
				100% {
					filter: blur(0);
					opacity: 1;
				}
			}

			.focus-in-website {
				animation: focus-in-expand 1.2s
					cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
			}

			@keyframes focus-in-expand {
				0% {
					filter: blur(14px);
					opacity: 0;
				}
				100% {
					filter: blur(0);
					opacity: 1;
				}
			}
		</style>
	</head>

	<body>
		<main id="mainContent" class="main-content">
			<h1 id="title" class="focus-in-somiiux">SOMIIUX</h1>
			<button id="biografiaBtn" class="biografia-btn">
				BIIO
			</button>
		</main>

		<section id="biografia" class="biografia-section">
			<div class="biografia-container">
				<h2 class="biografia-title">Biografía</h2>
				<div class="biografia-content">
					<p>
						Aquí va tu contenido de biografía. Puedes hablar sobre
						tu trayectoria, experiencia, pasiones y lo que te motiva
						en el desarrollo web.
					</p>

					<p>
						Puedes agregar tantos párrafos como necesites para
						contar tu historia.
					</p>
				</div>

				<button id="volverBtn" class="volver-btn">
					Volver al inicio
				</button>
			</div>
		</section>

		<script is:inline>
			// 🔽 SCRIPT CORREGIDO - MÁS SIMPLE 🔽

			const body = document.body;
			const title = document.getElementById("title");
			const biografiaBtn = document.getElementById("biografiaBtn");
			const volverBtn = document.getElementById("volverBtn");
			const mainContent = document.getElementById("mainContent");
			const biografiaSection = document.getElementById("biografia");

			// detectamos se é un dispositivo táctil (móbil/tablet)
			const isTouch = matchMedia(
				"(hover: none), (pointer: coarse)",
			).matches;

			if (!isTouch) {
				title.addEventListener("mouseenter", () => {
					body.classList.add("show-bg");
				});

				title.addEventListener("mouseleave", () => {
					body.classList.remove("show-bg");
				});
			} else {
				window.addEventListener("load", () => {
					setTimeout(() => {
						body.classList.add("show-bg");
					}, 3000);
				});
			}

			// Función para abrir biografía
			biografiaBtn.addEventListener("click", () => {
				console.log("Botón clickeado"); // Para debug

				body.classList.add("locked");
				mainContent.style.transform =
					"translate(-100%, -100%) scale(0.3)";
				mainContent.style.opacity = "0";

				setTimeout(() => {
					biografiaSection.classList.add("active");
					body.classList.remove("locked");
				}, 400);
			});

			// Función para cerrar biografía
			volverBtn.addEventListener("click", () => {
				body.classList.add("locked");
				biografiaSection.classList.remove("active");

				setTimeout(() => {
					mainContent.style.transform = "translate(0, 0) scale(1)";
					mainContent.style.opacity = "1";
					body.classList.remove("locked");
				}, 50);
			});

			// Cerrar con ESC
			document.addEventListener("keydown", (e) => {
				if (
					e.key === "Escape" &&
					biografiaSection.classList.contains("active")
				) {
					volverBtn.click();
				}
			});
		</script>
	</body>
</html>
